[package]
name = "rtmp_server_project"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1.0", features = ["full"] }
mongodb = "2.0"
openssl = "0.10"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
