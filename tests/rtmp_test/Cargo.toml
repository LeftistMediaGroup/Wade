[package]
name = "rtmp_server"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["full"] }
log = "0.4"
mongodb = "2.0"
bson = "2.0"
futures = "0.3"
serde = { version = "1", features = ["derive"] }
serde_json = "1.0"
bytes = "1.1"
openssl = "0.10"
