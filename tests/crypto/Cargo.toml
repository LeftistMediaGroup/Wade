[package]
name = "crypto1"
version = "0.1.0"
edition = "2021"

[dependencies]
aes = "0.7.5"  # Check for the latest version on crates.io
hex = "0.4"     # For encoding the encrypted bytes to a hex string
rand = "0.8"    # Optional, only if you plan to use randomness later